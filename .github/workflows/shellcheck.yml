name: Shellcheck

on:
  pull_request:
    paths:
      - "scripts/**"
  workflow_dispatch:

jobs:
  shellcheck:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
      - run: shellcheck scripts/install.sh
