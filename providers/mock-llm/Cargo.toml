[package]
name = "horsie-mock-llm"
license = "MIT OR Apache-2.0"
version = "0.1.6"
edition = "2024"
publish = false # a test harness entrypoint, never published

[dependencies]
async-llm = { workspace = true, features = ["mock"] }
tokio = { workspace = true, features = ["rt-multi-thread", "macros"] }

[lints]
workspace = true
